# 🛡️ L-PAD

**L-PAD** — это система биометрической защиты с функцией Flash Liveness. Проект разработан для предотвращения спуфинг-атак (фото/видео) с использованием активной световой проверки.

---
## Системные требования

| Компонент | Требование |
| :--- | :--- |
| **ОС** | Windows 10 / 11 (64-bit) |
| **Язык** | Python 3.10 |
| **Камера** | Веб-камера (Встроенная или USB) |

---

## Как запустить

Вы можете запустить проект двумя способами: используя готовый EXE-файл или исходный код Python.

### Вариант 1: Запуск через .EXE

1. Убедитесь, что у вас установлен **Python 3.10**.

2. Откройте терминал в папке проекта и установите ```pyinstaller```:

```bash
pip install pyinstaller
```

3. Чтобы создать .exe файл выпольните команду:

```bash
pyinstaller --noconfirm --onefile --windowed --name "L-PAD_Security" --add-data "data;data" --add-data "models;models" --additional-hooks-dir=. app.py
```

### Вариант 2: Запуск через Python

1. Убедитесь, что у вас установлен **Python 3.10**.

2. Откройте терминал в папке проекта и выполните команду:
```bash
pip install -r requirements.txt
```
3. Запустите программу:
```bash
python app.py
```



